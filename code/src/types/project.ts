export type Project = {
    /** Unique identifier for the nav item. */
    id: string;
    /** Name of the account holder. */
    name: string;
    /** API key of the account holder. */
    apiKey: string;
    /** Avatar image URL. */
    avatar?: string;
};
